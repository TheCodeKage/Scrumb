services:
  postgres:
    image: postgres:latest
    container_name: scrumb_postgres
    restart: always

    environment:
      POSTGRES_DB: scrumb
      POSTGRES_USER: scrumb_user
      POSTGRES_PASSWORD: strongpassword

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: